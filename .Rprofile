source("renv/activate.R")

# Set options for reproducibility
options(
  repos = c(CRAN = "https://cloud.r-project.org/"),
  renv.config.auto.snapshot = FALSE,
  stringsAsFactors = FALSE,
  encoding = "UTF-8"
)

# Set seed for reproducibility (can be overridden in scripts)
set.seed(20251012)
